#include <stdio.h>
int main(){
    long long sum,sn;
	int a,b,n;
	while(scanf("%d",&n)!=EOF){
	    sum=0;
        for(a=1;a<=n;a++){
            sn=1;
            for(b=1;b<=a;b++){
                sn*=b;
            }
            sum+=sn;
        }
        printf("%ld",sum);
    }
    return 0;
}
